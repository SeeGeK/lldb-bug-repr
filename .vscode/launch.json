{
   "version": "0.2.0",
   "configurations": [
      {
         "name": "lldb",
         "type": "lldb",
         "request": "launch",
         "program": "${command:cmake.buildDirectory}/lldb-bug",
         "cwd": "${command:cmake.buildDirectory}",
      }
   ]
}